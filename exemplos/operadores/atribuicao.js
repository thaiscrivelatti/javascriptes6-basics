var a, b;

// Atribuicao
a = b;

// Atribuicao de adicao
x = x + y;
x += y;

// Atribuicao de subtracao
x = x - y;
x -= y;

// Atribuicao de multiplicacao
x = x * y;
x *= y;

// Atribuicao de divisao
x = x / y;
x /= y;

// Atribuicao de resto
x = x % y;
x %= y;